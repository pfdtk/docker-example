version: '3'
services:
    nginx:
        image: "${NAME_SPACE}/nginx:v1.0"
        build:
            context: ./nginx
            dockerfile: Dockerfile
        volumes:
            - "$PWD/nginx/conf.d:/etc/nginx/conf.d"
            - "/Users/zzz/data/project:/var/www/html"
        ports:
            - "80:80"
        networks:
            - lnmp
        depends_on:
            - php
            - php6
    php:
        image: "${NAME_SPACE}/php:v1.0"
        build:
            context: ./php
            dockerfile: Dockerfile
        volumes:
            - "/Users/zzz/data/project:/var/www/html"
        networks:
            - lnmp
        depends_on:
            - mysql
    php6:
        image: "${NAME_SPACE}/php6:v1.0"
        build:
            context: ./php6
            dockerfile: Dockerfile
        volumes:
            - "/Users/zzz/data/project:/var/www/html"
        networks:
            - lnmp
        depends_on:
            - mysql
    phpcli:
        image: "${NAME_SPACE}/phpcli:v1.0"
        build:
            context: ./phpcli
            dockerfile: Dockerfile
        volumes:
            - "/Users/zzz/data/project:/var/www/html"
        networks:
            - lnmp
    phpcli6:
        image: "${NAME_SPACE}/phpcli6:v1.0"
        build:
            context: ./phpcli6
            dockerfile: Dockerfile
        volumes:
            - "/Users/zzz/data/project:/var/www/html"
        networks:
            - lnmp
    mysql:
        image: "${NAME_SPACE}/mysql:v1.0"
        build:
            context: ./mysql
            dockerfile: Dockerfile
        volumes:
            - mysql_data:/var/lib/mysql
        environment:
            TZ: 'Asia/Shanghai'
            MYSQL_ROOT_PASSWORD: 111111
        command: ['mysqld', '--character-set-server=utf8']
        networks:
            - lnmp
volumes:
    mysql_data:

networks:
    lnmp:
